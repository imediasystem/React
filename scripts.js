var UserTable = React.createClass({
    render: function() {
        return (
            <h2>To jest component UserTable</h2>
        );
    }
});

ReactDOM.render(<UserTable/>, document.getElementById('content'));